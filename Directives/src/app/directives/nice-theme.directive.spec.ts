import { NiceThemeDirective } from './nice-theme.directive';

describe('NiceThemeDirective', () => {
  it('should create an instance', () => {
    const directive = new NiceThemeDirective();
    expect(directive).toBeTruthy();
  });
});
