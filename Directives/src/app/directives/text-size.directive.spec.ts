import { TextSizeDirective } from './text-size.directive';

describe('TextSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new TextSizeDirective();
    expect(directive).toBeTruthy();
  });
});
