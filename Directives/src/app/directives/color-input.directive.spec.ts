import { ColorInputDirective } from './color-input.directive';

describe('ColorInputDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorInputDirective();
    expect(directive).toBeTruthy();
  });
});
